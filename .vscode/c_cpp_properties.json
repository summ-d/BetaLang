{
    "env": {
        "myIncludePath": [
            "${workspaceFolder}/lib",
            "${workspaceFolder}/include"
        ]
    },
    "configurations": [
        {
            "name": "Linux",
            "compilerPath": "/usr/bin/gcc",
            "includePath": [
                "${default}",
                "${workspaceFolder}/lib",
                "${workspaceFolder}/include"
            ]
        }
    ],
    "version": 4
}
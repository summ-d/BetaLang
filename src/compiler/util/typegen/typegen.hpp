#ifndef typegen_hpp
#define typegen_hpp

namespace util{

    enum ReservedKeywords{
        

    };

    enum PossibleArch{
        ARM,
        AVR,
        X86,
        X86_64,
        JVM,
        MIPS,
        RISC,
        MSIL,
        ERROR = -1
    };

}


#endif